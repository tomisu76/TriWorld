[project]
name = "triworld"
version = "0.1.0"
description = "One-click BeamNG map studio"
authors = [{name = "TriWorld Team"}]
dependencies = [
    "pydantic>=2.7.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
